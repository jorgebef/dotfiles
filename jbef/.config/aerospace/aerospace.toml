# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# Config version for compatibility and deprecations
# See: https://nikitabobko.github.io/AeroSpace/guide#config-version
# Fallback value (if you omit the key): config-version = 1
config-version = 2

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
# accordion-padding = 30
accordion-padding = 0

# List of workspaces that should stay alive even when they contain no windows,
# even when they are invisible.
# This config option is only available since 'config-version = 2'
# Fallback value (if you omit the key): persistent-workspaces = []
persistent-workspaces = ["1", "2", "3", "4"]

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
key-mapping.preset = 'qwerty'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = []
# on-focused-monitor-changed = ['move-mouse window-lazy-center']

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 6
inner.vertical =   6
outer.left =       6
outer.bottom =     6
outer.top =        6
outer.right =      6

# [key-mapping.key-notation-to-key-code]
# unicorn = 'u'

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

cmd-ctrl-comma = 'workspace prev'
cmd-ctrl-period = 'workspace next'
cmd-ctrl-shift-comma = 'move-node-to-workspace prev --focus-follows-window'
cmd-ctrl-shift-period = 'move-node-to-workspace next --focus-follows-window'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
# alt-h = 'focus left'
# alt-j = 'focus down'
# alt-k = 'focus up'
# alt-l = 'focus right'
cmd-ctrl-h = 'exec-and-forget aerospace focus --boundaries-action fail left || $HOME/.config/aerospace/scripts/focus-window.sh left'
cmd-ctrl-j = 'exec-and-forget aerospace focus --boundaries-action fail down || $HOME/.config/aerospace/scripts/focus-window.sh down'
cmd-ctrl-k = 'exec-and-forget aerospace focus --boundaries-action fail up || $HOME/.config/aerospace/scripts/focus-window.sh up'
cmd-ctrl-l = 'exec-and-forget aerospace focus --boundaries-action fail right || $HOME/.config/aerospace/scripts/focus-window.sh right'

cmd-ctrl-i = 'focus-monitor left'
cmd-ctrl-o = 'focus-monitor right'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
cmd-ctrl-shift-h = 'move left'
cmd-ctrl-shift-j = 'move down'
cmd-ctrl-shift-k = 'move up'
cmd-ctrl-shift-l = 'move right'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
cmd-ctrl-shift-minus = 'resize smart -50'
cmd-ctrl-shift-equal = 'resize smart +50'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
cmd-ctrl-1 = 'workspace 1'
cmd-ctrl-2 = 'workspace 2'
cmd-ctrl-3 = 'workspace 3'
cmd-ctrl-4 = 'workspace 4'

cmd-ctrl-shift-i = 'move-node-to-monitor --wrap-around prev --focus-follows-window'
cmd-ctrl-shift-o = 'move-node-to-monitor --wrap-around next --focus-follows-window'

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
cmd-ctrl-shift-1 = 'move-node-to-workspace 1 --focus-follows-window'
# cmd-ctrl-shift-2 = 'move-node-to-workspace 2 --focus-follows-window'
# cmd-ctrl-shift-3 = 'move-node-to-workspace 3 --focus-follows-window'
# cmd-ctrl-shift-4 = 'move-node-to-workspace 4 --focus-follows-window'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
cmd-ctrl-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
cmd-ctrl-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
cmd-ctrl-semicolon = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
shift-r = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
# backspace = ['close-all-windows-but-current', 'mode main']
cmd-ctrl-semicolon = 'mode main'

# # See: https://nikitabobko.github.io/AeroSpace/commands#layout
# slash = 'layout tiles horizontal vertical'
enter = ["fullscreen", "mode main"]
slash = ["layout tiles accordion", "mode main"]


[mode.normal.binding]
cmd-ctrl-semicolon = 'mode main'
# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']

# alt-shift-h = ['join-with left', 'mode main']
# alt-shift-j = ['join-with down', 'mode main']
# alt-shift-k = ['join-with up', 'mode main']
# alt-shift-l = ['join-with right', 'mode main']


# INFO: find app-id by running:
# osascript -e 'id of app "<name of the app>"'

# Float qBittorrent windows
[[on-window-detected]]
if.app-id = 'com.apple.iCal'
run = ['layout floating']

# Float Ollama windows
[[on-window-detected]]
if.app-id = 'com.electron.ollama'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'net.whatsapp.WhatsApp'
run = ['layout floating']

# Float Apple Music windows
[[on-window-detected]]
if.app-id = 'com.apple.Music'
run = ['layout floating']

# Float Notes windows
[[on-window-detected]]
if.app-id = 'com.apple.Notes'
run = ['layout floating']

# Float Spotify windows
[[on-window-detected]]
if.app-id = 'com.spotify.client'
run = ['layout floating']

# Float Finder windows
[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = ['layout floating']

# Float wulkano windows
[[on-window-detected]]
if.app-id = 'com.wulkano.kap'
run = ['layout floating']

# Float Ghostty windows
[[on-window-detected]]
if.app-id = 'com.mitchellh.ghostty'
if.window-title-regex-substring = 'Updat'
run = ['layout floating']

# Float Activity Monitor windows
[[on-window-detected]]
if.app-id = 'com.apple.ActivityMonitor'
run = ['layout floating']

# Float Obsidian windows
[[on-window-detected]]
if.app-id = 'md.obsidian'
run = ['layout floating']

# Float Discord windows
[[on-window-detected]]
if.app-id = 'com.hnc.Discord'
run = ['layout floating']

# Float 1Password windows
[[on-window-detected]]
if.app-id = 'com.1password.1password'
run = ['layout floating']

# Float Bitwarden windows
[[on-window-detected]]
if.app-id = 'com.bitwarden.desktop'
run = ['layout floating']

# Float BitWarden Chrome windows
[[on-window-detected]]
if.app-id = 'com.google.Chrome'
# if.window-title-regex-substring = '^Bitwarden$'
if.window-title-regex-substring = 'bitwarden'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'com.zen-browser.zen'
if.window-title-regex-substring = 'Exten'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'org.qbittorrent.qBittorrent'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'com.apple.weather'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'app.yaak.desktop'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'ai.elementlabs.lmstudio'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'com.nousresearch.hermes'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'com.lukilabs.lukiapp'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'com.youssofal.mtplx'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'app.omlx'
run = ['layout floating']

[[on-window-detected]]
if.app-id = 'com.tinycast.app'
run = ['layout floating']
