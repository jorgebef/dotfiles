# NOTE: you can use vars with $<var> and ${<var>} as long as the str is double quoted: ""
# WARNING: hex colors can't contain capital letters
# --> Catppuccin
# --> Catppuccin (Mocha)
thm_bg="#1e1e2e"
thm_fg="#cdd6f4"
thm_cyan="#89dceb"
thm_black="#181825"
thm_gray="#313244"
thm_magenta="#cba6f7"
thm_pink="#f5c2e7"
thm_red="#f38ba8"
thm_green="#a6e3a1"
thm_yellow="#f9e2af"
thm_blue="#89b4fa"
thm_orange="#fab387"
thm_black4="#585b70"
thm_overlay0="#6c7086"
thm_overlay2="#9399b2"
thm_subtext0="#a6adc8"
# ----------------------------=== Theme ===--------------------------

set -g default-terminal "${TERM}" # Upgrade $TERM
# set -g terminal-overrides ",xterm-256color:RGB"
# set -g default-terminal "screen-256color"

set -g base-index 1          # start indexing windows at 1 instead of 0
set -g detach-on-destroy off # don't exit from tmux when closing a session
set -g escape-time 0         # zero-out escape time delay
set -g history-limit 3000   # increase history size (from 2,000)
set -g mouse on              # enable mouse support
set -g renumber-windows on   # renumber all windows when any window is closed
set -g set-clipboard on      # use system clipboard
# Emacs key bindings in tmux command prompt (prefix + :) are better than
# vi keys, even for vim users
set -g status-keys emacs
# Focus events enabled for terminals that support them
set -g focus-events on
# Super useful when using "grouped sessions" and multi-monitor setup
setw -g aggressive-resize on

set-option -g automatic-rename on

set -s extended-keys on

unbind-key -a

# ********************************************************
# ********** Improving existing keybindings **************
# ********************************************************
bind-key -r C-v copy-mode # Start vim-like navigation
# bind-key -n þ run-shell "~/.config/tmux/plugins/t-smart-tmux-session-manager/bin/t"

bind-key -n þ run-shell "sesh connect $(
sesh list -t | fzf-tmux -p 55%,60% \
  --no-sort --border-label ' sesh ' --prompt '⚡  ' \
  --header '  ^a all ^t tmux ^x zoxide ^f find' \
  --bind 'tab:down,btab:up' \
  --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list)' \
  --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t)' \
  --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z)' \
  --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)'
)"

# bind-key -n þ run-shell -b "$HOME/.config/tmux/plugins/tmux-fzf/scripts/session.sh switch"

# bind-key -n  resize-pane -Z
# bind-key -n M-C-m choose-tree -swZ

bind * list-clients
bind : command-prompt # Enter interactive prompt
bind-key -r N command-prompt -p "New Session:" "new-session -A -s '%%'" # Create new session and prompt for name
bind r command-prompt "rename-window %%"
# bind c new-window -c '#{pane_current_path}'
# bind '"' split-window -c '#{pane_current_path}'
# bind % split-window -h -c '#{pane_current_path}'

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R

bind * list-clients
bind : command-prompt
bind-key -r N command-prompt -p "New Session:" "new-session -A -s '%%'"
bind r command-prompt "rename-window %%"
bind c new-window -c '#{pane_current_path}' -a -t '{next}'

bind 'C-d' detach
bind h split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

bind-key -r = select-layout even-horizontal

# bind-key -n Ω next-window
# bind-key -n † previous-window
bind-key -n C-. select-window -t +1
bind-key -n C-, select-window -t -1
bind-key -n † resize-pane -Z
bind-key -n Ω if-shell -F '#{pane_index}==0' \
              { select-pane -t 0 ; resize-pane -Z } \
              { select-pane -t 0 ; resize-pane -y 40 ; select-pane -D }

bind r {
  source-file ~/.config/tmux/tmux.conf
  display 'config reloaded'
}

bind -r R command-prompt -p "Rename Session: " "rename-session '%%'"

# set -g window-status-current-format "#[fg=$thm_blue,bold] 󰄛󰏩󰩃 #[fg=$thm_subtext0]"
# set -g window-status-format "#[fg=$thm_overlay0]———"

set -g window-status-current-format "#[fg=$thm_blue,bold]#W "
set -g window-status-format "#[fg=$thm_overlay0]#W "

set -g status-interval 1     # update the status bar every 3 seconds
set -g status-left "#[fg=$thm_lavender,bold]  #S  #[fg=$thm_overlay0]|  "
set -g status-left-length "100"
set -g status-right-length "100"
set -g status-position bottom
set -g status-right "#[fg=$thm_overlay2,bold]#{?window_zoomed_flag,,} #[fg=$thm_overlay0]| #(gitmux -cfg $HOME/.config/tmux/gitmux.conf #{pane_current_path})"

set -g status-style 'bg=default'

# # POPUP SHELL
# bind -n C-n display-popup -T ' +#S ' -h 60% -E show-tmux-popup.sh
#
# set -g popup-border-style fg=colour245
# set -g popup-border-lines rounded
#
# # support detaching from nested session with the same shortcut
# bind -T popup C-n detach
# bind -T popup C-o copy-mode
# bind -T popup M-c new-window -c '#{pane_current_path}'
# bind -T popup M-n next-window
# bind -T popup M-m previous-window
#
# bind -T popup M-L run 'tmux move-window -a -t $TMUX_PARENT_SESSION:{next}'

# panes
set -g pane-border-style "fg=${thm_black4}"
set -g pane-active-border-style "fg=magenta"

# # Undercurl
# set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
# set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0

TMUX_FZF_OPTIONS="-p -w 65% -h 60% -m"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'christoomey/vim-tmux-navigator'
# run '~/.tmux/plugins/tpm/tpm'
# set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-capture-pane-contents 'off'


# set -g @plugin 'catppuccin/tmux'
# set -g @resurrect-strategy-nvim 'session'
# set -g @resurrect-processes 'ssh'
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
# run -b '~/.config/tmux/plugins/tpm/tpm'
run -b '~/.local/share/tmux/plugins/tpm/tpm'
